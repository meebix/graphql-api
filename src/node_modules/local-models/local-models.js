// User
const userMethods = require('./user/user-methods');
const userValidations = require('./user/user-validations');
const userFormatters = require('./user/user-formatters');

// Combine
const validations = Object.assign({}, userValidations);
const formatters = Object.assign({}, userFormatters);

module.exports = {
  // Models
  userMethods,

  // Validations & Formatters
  validations,
  formatters,
};
